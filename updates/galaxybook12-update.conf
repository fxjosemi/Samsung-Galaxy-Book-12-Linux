# Components maintained for every selected CachyOS kernel update.
COMPONENTS="audio brightness cameras sensors"

# Keep a different kernel family as an unmodified recovery option. The installer
# replaces this value with the pkgbase of the kernel that is running.
KERNEL_PACKAGES="linux-cachyos"

# Conservative default for the tablet's memory and cooling.
JOBS=2
